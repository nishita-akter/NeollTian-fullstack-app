def show_message(role, message):
    """(Deprecated) Streamlit UI replaced by React frontend."""
    # React will now handle message display.
    # This function remains to avoid breaking imports.
    pass
